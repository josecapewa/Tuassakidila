<?php
/**
 * Configurações de conexão com o banco de dados
 *
 * As constantes abaixo definem os parâmetros necessários para estabelecer
 * uma conexão com o banco de dados. Elas incluem o host do banco, o nome
 * de usuário, a senha e o nome da base de dados.
 *
 * Estas constantes são utilizadas pela classe de operações com o banco
 * de dados para configurar corretamente a conexão.
 *
 * - host: Endereço do servidor do banco de dados (por exemplo, 'localhost' ou IP).
 * - user: Nome de usuário para autenticação no banco de dados.
 * - pass: Senha correspondente ao usuário definido.
 * - dbname: Nome da base de dados a ser acessada.
 */
define( 'host', '' );
define( 'user', '' );
define( 'pass', '' );
define( 'dbname', '' );
